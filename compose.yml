services:
  kleinanzeigen-agent:
    build: .
    image: kleinanzeigen-agent:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # persistent work/queue dirs and config survive container restarts
      - /opt/kleinanzeigen-agent/data:/data
